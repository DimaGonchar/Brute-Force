#pragma once
#include<iostream>
#include<fstream>

/*void WritePass(const std::string& pass) {
    const std::string filePath = "passList";
    std::ofstream file(filePath,std::ios::app);
    file << pass << std::endl;
    file.close();
}
void WritePass(const std::vector<std::string>&vecPass) {
    const std::string filePath = "passList";
    std::ofstream file(filePath, std::ios::app);
    for (int i = 0; i < vecPass.size(); ++i) {
        file << vecPass[i]<<std::endl;
    }
    file.close();
}*/
//std::vector<std::string>verifiedPasswords;


